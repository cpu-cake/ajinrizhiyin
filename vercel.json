{
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "{server/**,drizzle/**,shared/**,package.json,pnpm-lock.yaml,tsconfig.json}"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ],
  "outputDirectory": "dist/public",
  "buildCommand": "npm run build"
}
