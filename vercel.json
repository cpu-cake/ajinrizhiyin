{
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "{server/**,drizzle/**,shared/**,package.json,yarn.lock,tsconfig.json}"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ],
  "outputDirectory": "dist/public",
  "buildCommand": "npm run build"
}
